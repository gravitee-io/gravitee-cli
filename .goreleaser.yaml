# Copyright (C) 2015 The Gravitee team (http://gravitee.io)
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

version: 2

project_name: gctl

builds:
  - id: gctl
    binary: gctl
    main: .
    env: [CGO_ENABLED=0]
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
  - id: gctl-ro
    binary: gctl-ro
    main: ./cmd/gctl-ro
    env: [CGO_ENABLED=0]
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: gctl
    ids: [gctl]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [LICENSE.txt, README.md]
  - id: gctl-ro
    ids: [gctl-ro]
    name_template: "{{ .ProjectName }}_readonly_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [LICENSE.txt, README.md]

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  use: github
  sort: asc
  groups:
    - title: Features
      regexp: '^feat(\(.+\))?!?:'
      order: 0
    - title: Bug fixes
      regexp: '^fix(\(.+\))?!?:'
      order: 1
    - title: Other changes
      order: 999
  filters:
    exclude: ['^docs', '^chore', '^test', '^ci', '^style', '^refactor']

release:
  header: |
    ## gctl {{ .Tag }}

    gctl is the Gravitee CLI for APIM & AM.
  footer: |
    ## Variants

    This release ships two binaries:

    | Binary | Use case | Commands |
    |--------|----------|----------|
    | `gctl` | Humans, scripts, CI | Full CRUD - get, list, create, update, delete |
    | `gctl-ro` | AI agents, read-only automation | Get and list only - no write operations |

    Both binaries read the same config file (`~/.gctl/config.yaml`) and support the same flags and output formats. Install both on the same machine if you want agents to use `gctl-ro` while you use `gctl` - they coexist without conflict.

    ## Install

    ### curl (macOS / Linux, and Windows via Git Bash / WSL)

    ```bash
    curl -fsSL https://raw.githubusercontent.com/gravitee-io/gravitee-cli/main/install.sh | sh
    ```

    Install the read-only variant instead with `GCTL_BIN=gctl-ro`:

    ```bash
    curl -fsSL https://raw.githubusercontent.com/gravitee-io/gravitee-cli/main/install.sh | GCTL_BIN=gctl-ro sh
    ```

    ### Manual (any OS, including native Windows)

    Download the archive for your platform from the **Assets** below, extract it, and move `gctl` (and optionally `gctl-ro`) into a directory on your `PATH`. Run `gctl version` to verify.
