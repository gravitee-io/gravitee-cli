# Copyright (C) 2015 The Gravitee team (http://gravitee.io)
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

version: 2

project_name: gctl

builds:
  - id: gctl
    binary: gctl
    main: .
    env: [CGO_ENABLED=0]
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
  - id: gctl-ro
    binary: gctl-ro
    main: ./cmd/gctl-ro
    env: [CGO_ENABLED=0]
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: gctl
    ids: [gctl]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [LICENSE.txt, README.md]
  - id: gctl-ro
    ids: [gctl-ro]
    name_template: "{{ .ProjectName }}_readonly_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [LICENSE.txt, README.md]

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  use: github
  sort: asc
  groups:
    - title: Features
      regexp: '^feat(\(.+\))?!?:'
      order: 0
    - title: Bug fixes
      regexp: '^fix(\(.+\))?!?:'
      order: 1
    - title: Other changes
      order: 999
  filters:
    exclude: ['^docs', '^chore', '^test', '^ci', '^style', '^refactor']

release:
  header: |
    ## gctl {{ .Tag }}

    gctl is the Gravitee CLI for APIM & AM.
  footer: |
    ## Variants

    This release ships two binaries:

    | Binary | Use case | Commands |
    |--------|----------|----------|
    | `gctl` | Humans, scripts, CI | Full CRUD - get, list, create, update, delete |
    | `gctl-ro` | AI agents, read-only automation | Get and list only - no write operations |

    Both binaries read the same config file (`~/.gctl/config.yaml`) and support the same flags and output formats. Install both on the same machine if you want agents to use `gctl-ro` while you use `gctl` - they coexist without conflict.

    ## Install

    ### Linux / macOS

    **1. Identify your platform.** Run `uname -sm` in a terminal and match the output:

    | `uname -sm` output | `gctl` archive | `gctl-ro` archive |
    |--------------------|---------------|------------------|
    | `Darwin arm64`     | [`gctl_{{ .Version }}_darwin_arm64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_{{ .Version }}_darwin_arm64.tar.gz) | [`gctl_readonly_{{ .Version }}_darwin_arm64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_readonly_{{ .Version }}_darwin_arm64.tar.gz) |
    | `Darwin x86_64`    | [`gctl_{{ .Version }}_darwin_amd64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_{{ .Version }}_darwin_amd64.tar.gz) | [`gctl_readonly_{{ .Version }}_darwin_amd64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_readonly_{{ .Version }}_darwin_amd64.tar.gz) |
    | `Linux x86_64`     | [`gctl_{{ .Version }}_linux_amd64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_{{ .Version }}_linux_amd64.tar.gz) | [`gctl_readonly_{{ .Version }}_linux_amd64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_readonly_{{ .Version }}_linux_amd64.tar.gz) |
    | `Linux aarch64`    | [`gctl_{{ .Version }}_linux_arm64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_{{ .Version }}_linux_arm64.tar.gz) | [`gctl_readonly_{{ .Version }}_linux_arm64.tar.gz`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_readonly_{{ .Version }}_linux_arm64.tar.gz) |

    **2. Download and extract** (replace `linux_amd64` with your archive):

    ```bash
    tar -xzf gctl_{{ .Version }}_linux_amd64.tar.gz
    ./gctl version
    ```

    Move the resulting binary to a directory in your `PATH`. If you install both variants, both `gctl` and `gctl-ro` can live in the same directory.

    ### Windows

    1. Download [`gctl_{{ .Version }}_windows_amd64.zip`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_{{ .Version }}_windows_amd64.zip) and/or [`gctl_readonly_{{ .Version }}_windows_amd64.zip`](https://github.com/gravitee-io-labs/gctl/releases/download/{{ .Tag }}/gctl_readonly_{{ .Version }}_windows_amd64.zip)
    2. Unzip into a folder of your choice (e.g. `C:\Tools\gctl`)
    3. Add that folder to your `PATH` so `gctl` works from any terminal (run once in PowerShell):

       ```powershell
       $target = "C:\Tools\gctl"
       $userPath = [Environment]::GetEnvironmentVariable("PATH", "User")
       if (-not ($userPath -split ";" -contains $target)) {
           [Environment]::SetEnvironmentVariable("PATH", "$userPath;$target", "User")
       }
       ```

    4. Open a new terminal and verify:

       ```powershell
       gctl version
       ```
